// FlowMap is deck.gl — strictly client-only (it touches window/WebGL at import).
// Prerender the static shell, but render the body only in the browser.
export const prerender = true;
export const ssr = false;
