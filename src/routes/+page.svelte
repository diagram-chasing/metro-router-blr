<script lang="ts">
	// Homepage: the full-colour WebGL flow-map wall piece. Animated commute trails
	// over a live, breathing emissions cloud, with a counterfactual toggle. The old
	// grey accumulation map (TouchDesigner feed) now lives at /accumulate.
	import FlowMap from '$lib/components/FlowMap.svelte';
</script>

<main class="absolute inset-0">
	<FlowMap variant="home" />
</main>
