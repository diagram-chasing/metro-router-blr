node_modules

# Claude Code / agent tooling (worktrees, skills) — not app code
.claude/
.agents/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Local exhibit database (accumulates lines + responses at runtime)
/data

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Generated receipt permutation report (pnpm receipts:matrix)
scripts/out/

# OTP heavy artifacts — generated street graph + downloaded server binary (exceed GitHub's 100MB limit; rebuilt/redownloaded locally)
otp/data/graph.obj
otp/otp-shaded-2.9.0.jar

# Vendored reference dumps (downloaded libraries/shaders, kept locally only — not app code)
docs/
